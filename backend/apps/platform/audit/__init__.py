"""AuditEvent and audit emission helpers (G.5).

Phase 1: app skeleton only — concrete AuditEvent and audit_emit() land
in M1 alongside the first state-changing service.
"""

default_app_config = "apps.platform.audit.apps.AuditConfig"
